# lint requirements
-r requirements-lint.txt
# build requirements
Cython
cmake>=3.26
# runtime requirements
cffi
cpplint
Cython
docutils
dtlib
numpy>=1.23.5
pytest>=6.2.4
pytest_xdist>=2.2.1
pytest-durations
pytest-timeout
packaging>=21.0
PyYAML
tqdm>=4.62.3
typing_extensions>=4.10.0
requests
cloudpickle
ml_dtypes
psutil
# PyTorch - installs CUDA version by default (~6GB)
# For TT backend, CPU-only PyTorch is sufficient and saves ~5GB
# The build script automatically replaces this with CPU-only version
torch
tabulate
wheel
setuptools
einops
scipy
tornado
